"""Setup script for Group Travel Optimiser."""
from setuptools import setup, find_packages

setup(
    name="group-travel-optimiser",
    version="1.0.0",
    description="Internal corporate tool for comparing workshop locations and dates",
    packages=find_packages(),
    python_requires=">=3.11",
)
